const defaultState = {
    pageTitle: 'Home',
}

const uiReducer = (state = defaultState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default uiReducer;